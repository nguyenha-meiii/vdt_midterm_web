apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  labels:
    app: {{ .Values.appName }}
spec:
  type: NodePort
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: {{ .Values.appName }}
    tier: frontend