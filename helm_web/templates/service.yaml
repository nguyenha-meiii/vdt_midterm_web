apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}
  labels:
    app: {{ .Values.appName }}
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 80
    nodePort: 30100
  selector:
    app: {{ .Values.appName }}
    tier: frontend